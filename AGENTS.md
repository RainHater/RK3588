# AGENTS.md

## 最高优先级总则
- 必须遵守: 每当用户分配任何任务时, 都必须先严格对照并执行本 relu 的全部规范, 不得跳过、简单或忽略任何一条。
- 必须遵守: 执行前先按照“执行规范”确认需求、有疑问先问、按规范选择工具子代理，确认后再动手。

## 项目信息
- 必须遵守: **每次用户让你写代码、写 `docs/plan.md` 、写 `docs/spec.md` 时**你都需要读取 `docs/request.md`

## 当前项目软件架构
- 写代码时按照 `docs/嵌入式软件分层架构.md` 的规范来进行编写分层。
- 之后写代码、增加功能需求注意软件框架的可靠性、可维护性、可扩展性、解耦分层、**简单实用性**。
- 禁止出现业务层调用底层这种跨层调用行为, 比如**系统层不得直接调用数据层**, 必须经应用层协调

## 代码注释规则
- 不需要详细注释, 只需要每一个头文件函数加上 // 函数简略功能

## Task Execution
- 必须遵守: 当前用户让你执行任务时, 你不能立即执行, 你需要先告诉用户你理解的需求是什么, 等待用户确认后你再开始操作。
- 必须遵守: 如果你有任何不确定的问题, 请主动询问用户, 不要猜测。
- 必须遵守: 用户没有让你改的、增添的内容, 你都不能改动。比如用户让你写一个函数, 你只能写这个函数, 不能顺便把其他函数也重写了。
- 必须遵守: 当你修改代码完成一个需求时，你需要总结做了什么/哪些没做/留了什么坑给客户。

## Development And Testing
**必须遵守以下规则:**
- 执行用户要求的任务(如写代码、写 `docs/plan.md` 以及 `docs/spec.md` 文档)时, 必须调用 `code-reviewer` 代理去审查, 确保能达到预期效果。
- If the testing subagent finds bugs, fix them before considering the task complete.

## 调用 `code-reviewer` 代理审查规范
当用户使用 `code-reviewer` 代理审查代码、spec、plan 时, 必须遵守以下规范:
- 审查是否符合设计方案, 是否能完成用户预期目标
- 审查是否存在 bug
- 审查代码风格是否符合项目要求
- 审查是否解耦分层
- 审查代码是否存在冗余, 有死代码的出现
- 审查代码是否简单实用、可靠性强、易维护、易扩展

给用户看的 `docs/report.md` 文件用中文显示
